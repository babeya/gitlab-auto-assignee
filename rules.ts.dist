const rules = {
  "projects": [
    {
      "projectIds": [1],
      "groupId": 1,
      "rules": [
        { "branch": ["all"], "minLevel": 30, "nbReviewers": 2 },
        { "branch": ["master"], "minLevel": 40, "nbReviewers": 2 }
      ]
    }
  ]
}

export default rules;
